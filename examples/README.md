# Examples

Executable optimization examples will be added only after the corresponding public APIs are implemented, numerically verified, documented, and covered by tests.

The current package foundation does not yet provide models, posteriors, samplers, acquisition functions, or candidate optimizers, so this directory intentionally contains no executable optimization example.
